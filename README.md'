AUTHENTICATION BACK-END APP BUILT WITH NODE, EXPRESS AND MONGODB.

This app allows users to authenticate to the server using node js, expres and mongoDB.

It can be easily integrated with any web app to store username and password through a token generation (SHA256).

You can test it out with a POSTMAN.
